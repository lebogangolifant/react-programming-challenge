import React from 'react';

export const SolutionThree = () => {
  return (
    <div>
      create an array displayed in a table with each row containing the name of
      the object and a button to open a drop-down showing a list containing the
      individual fields and their values
    </div>
  );
};

export default SolutionThree;
